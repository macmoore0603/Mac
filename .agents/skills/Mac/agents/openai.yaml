interface:
  display_name: "Mac"
  short_description: "Repo-specific patterns and workflows for Mac"
  default_prompt: "Use the Mac repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true